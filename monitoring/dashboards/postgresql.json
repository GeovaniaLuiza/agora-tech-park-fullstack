{
  "annotations": { "list": [] }, "editable": true, "graphTooltip": 1,
  "panels": [
    { "type": "stat", "title": "Banco disponível", "id": 1, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "targets": [{ "refId": "A", "expr": "agora_database_up" }], "fieldConfig": { "defaults": { "mappings": [{ "type": "value", "options": { "0": { "text": "DOWN", "color": "red" }, "1": { "text": "UP", "color": "green" } } }] }, "overrides": [] }, "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 } },
    { "type": "timeseries", "title": "Conexões por estado", "id": 2, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "targets": [{ "refId": "A", "expr": "agora_database_connections", "legendFormat": "{{state}}" }], "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] }, "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 } },
    { "type": "stat", "title": "Tamanho do banco", "id": 3, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "targets": [{ "refId": "A", "expr": "agora_database_size_bytes" }], "fieldConfig": { "defaults": { "unit": "bytes" }, "overrides": [] }, "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 } },
    { "type": "timeseries", "title": "Transações", "id": 4, "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }, "targets": [{ "refId": "A", "expr": "rate(pg_stat_database_xact_commit[5m])", "legendFormat": "commit" }, { "refId": "B", "expr": "rate(pg_stat_database_xact_rollback[5m])", "legendFormat": "rollback" }], "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] }, "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 } }
  ],
  "refresh": "1m", "schemaVersion": 41, "tags": ["agora", "postgresql"],
  "templating": { "list": [{ "name": "DS_PROMETHEUS", "type": "datasource", "query": "prometheus", "label": "Prometheus" }] },
  "time": { "from": "now-6h", "to": "now" }, "title": "Ágora — PostgreSQL", "uid": "agora-postgresql", "version": 1
}

